# 学習ロードマップ（Phase 0〜6）

前回（家計簿版）のロードマップから変更なし。差分は2点のみ。

| Phase | 内容 | 家計簿版からの差分 |
|---|---|---|
| 0 | 環境構築 | 変更なし |
| 1 | プロジェクト作成 | 変更なし |
| 2 | モデル定義・Admin 登録 | **モデルが3つになる**（Category / Transaction / Client） |
| 3 | 一覧・登録画面 | 変更なし |
| 4 | 編集・削除 | 変更なし |
| 5 | 検索・フィルタ | ※ 2モデルで始めた場合、この前後で Client を追加 |
| 6 | グラフ・集計 | **グラフが多彩になる**（→ `02_charts.md`） |

## 次のステップ
Phase 2 として `Category` と `Transaction`（＋必要なら `Client`）の `models.py` 実コードと Admin 登録から書き始める。

**要決定**: 最初から3モデルで行くか、まず2モデルで小さく始めるか（→ `01_data_model.md` の実装順序）。
